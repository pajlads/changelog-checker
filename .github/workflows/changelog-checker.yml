---
name: Changelog Checker

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  changelog-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: ./
